import React from 'react';

const Chip = ({ label }) => <p className='text-xs bg-gradient-to-r from-blue-500 to-blue-200 text-white px-2 py-1 rounded-md w-fit uppercase'>{label}</p>;

export default Chip; 